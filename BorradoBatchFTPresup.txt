SET NOCOUNT ON;
GO
DECLARE @r INT;
SET @r = 1;
WHILE @r > 0
BEGIN
 BEGIN TRANSACTION;
 DELETE TOP (250000)
 FIN_FT_PRESUPUESTO
 WHERE (FIN_PSTO_ANIO_FISCAL = year(getdate()) OR FIN_PSTO_ANIO_FISCAL = year(getdate())-1);
 SET @r = @@ROWCOUNT;
 COMMIT TRANSACTION;
END
GO
SET NOCOUNT OFF;
GO